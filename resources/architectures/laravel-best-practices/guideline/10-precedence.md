Laravel best practices are the baseline Laravel guidance for this project.

- Follow local project conventions first.
- Follow enabled Architecture Kit rules before generic Laravel advice.
- If generic Laravel advice conflicts with an enabled architecture rule, the enabled architecture rule wins.
- This section is intentionally rendered after specific architectures so agents see it as a baseline, not as an override.
