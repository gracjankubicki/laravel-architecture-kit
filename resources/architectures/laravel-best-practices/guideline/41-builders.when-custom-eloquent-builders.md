Custom Eloquent Builders:

- Reusable model-specific query language belongs in Custom Eloquent Builders. See `architecture-kit-custom-eloquent-builders`.
- Do not duplicate the same local scopes or query snippets across unrelated classes.
